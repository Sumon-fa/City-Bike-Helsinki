import React from 'react'
import LinearProgress from '@mui/material/LinearProgress'

export default function LinearLoader() {
  return <LinearProgress sx={{ display: 'flex', margin: 'auto', width: '80%' }} color='secondary' />
}
